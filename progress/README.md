# Session progress logs

This folder is written to by the `save` skill at the end of each session.

The `recall` skill reads the most recent file at the start of each new session
to restore context.

Filenames: `YYYY-MM-DD_HHMM.md`. Sorted = chronological.

(This folder is empty until your first `save` in this project.)

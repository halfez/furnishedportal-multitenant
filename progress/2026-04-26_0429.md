# Session — 2026-04-26 (migrated from Yellowstone SaaS)

This is the bootstrapped session log for the FurnishedPortal-Product project. The prior work history
lived in the "Yellowstone SaaS" Claude project (now archived). This file captures the state at handoff.

---

## Where things stand

**The product:** A white-label Next.js 14 MTR (midterm rental) portal. Afshin sells this as a service —
each paying host client gets a deployment on their own domain. The original instance is yellowstone2.com.

**Codebase location:** `template/` subfolder (not at project root). Prefix all paths with `template/`.

**Git:** One commit ("initial commit") on the current branch. No incremental feature work has been
committed yet on top of the scaffold.

**Current placeholder client:** `template/config/property.ts` is filled with "Sunset Suites" / Maria
Gonzalez in Austin. This is sample data — not a real client.

**Routes scaffolded:** admin, auth, billing, bookings, chatbot, contact, FAQ, gallery, signup, Stripe,
tenant — all exist as route files but vary in completion.

---

## Open roadmap items (in rough priority order)

### High priority
1. **Abacus.AI → OpenAI migration (chatbot)** — The chatbot currently calls Abacus.AI. Migrate to
   OpenAI API. This is the highest-leverage migration because it's the most visible feature to tenants.

2. **Neon DB provisioning** — Database isn't provisioned yet. Prisma schema exists but no connection string.
   Go to neon.tech, create a project, copy the connection string into `template/.env.local`.

3. **NextAuth setup** — Tenant auth is scaffolded but not wired to a real DB. Depends on Neon DB.

### Medium priority
4. **Abacus.AI → Node library (PDF generation)** — Replace Abacus.AI-based PDF generation with a
   Node library (e.g. pdfkit or puppeteer).

5. **Abacus.AI → Resend (email)** — Migrate email sending to Resend everywhere.

6. **E-signature audit trail** — Not built yet. Required for lease agreements to be legally sound.

7. **Reviews system** — No reviews/testimonials system exists yet.

### SaaS delivery artifacts (needed to scale onboarding)
8. **Sub-30-min deployment checklist** — A step-by-step doc for spinning up a new client site fast.

9. **Client intake form** — A plain-language form that collects all info needed to fill `property.ts`,
   without requiring Afshin to hand-edit config files.

10. **Client welcome package PDF** — What a new client receives after signing up.

---

## Stale leftovers to clean up (low priority)
- `template/CLAUDE.md` and `template/.claude/launch.json` — pre-split artifacts, won't break anything
  but add clutter. Delete when convenient.
- Empty folder at `template/app/api/billing/checkout/` with NOTICE.md inside — safe to delete manually.
